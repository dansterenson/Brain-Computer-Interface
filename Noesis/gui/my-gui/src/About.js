import React from 'react';
import './App.css';

function About() {
    return (
        <div>
            <h3 className=titles>A website which visualizes cognition snapshots that were uploaded to the server, where multiple parsers read the snapshots, parsed various parts of it, and publish the parsed results, which are then saved to a database and you can view them on this website.

</h3>
        </div>
    );
}

export default About;
